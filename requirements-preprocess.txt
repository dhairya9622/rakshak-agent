# Preprocessing only (run once, locally, to build the knowledge base):
pdfplumber>=0.11

# The agent itself (rakshak_agent/*) has ZERO runtime dependencies - it uses
# only the Python standard library, so it runs fully offline.
#
# Optional, only if you enable the real LLM escalation tier:
#   - no extra package needed; DeepSeekClient uses urllib from the stdlib.
#   - set DEEPSEEK_API_KEY in the environment to activate it.
